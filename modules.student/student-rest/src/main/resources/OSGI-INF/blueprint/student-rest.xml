<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xsi:schemaLocation="http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
       http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">

    <reference id="studentService" interface="cn.com.rexen.bean.api.biz.IStudentBeanService"/>

    <camelContext id="studentCamel" xmlns="http://camel.apache.org/schema/blueprint">

        <restConfiguration component="servlet" bindingMode="json">
            <dataFormatProperty key="prettyPrint" value="true"/>
        </restConfiguration>

        <!-- defines the rest services using the context-path /student -->
        <rest path="/students" consumes="application/json;charset=utf-8" produces="application/json;charset=utf-8">
            <description>student rest service</description>

            <!-- this is a rest GET to find all student -->
            <get id="route-getAllStudent" outType="cn.com.rexen.core.api.persistence.JsonData">
                <description>Find all Students</description>
                <to uri="bean:studentService?method=getAllEntityByQuery(${header.page},${header.limit},${header.jsonStr})"/>
            </get>

            <!-- this is a rest POST to create a student -->
            <post id="route-addStudent" type="cn.com.rexen.bean.entities.StudentBean"
                  outType="cn.com.rexen.core.api.biz.JsonStatus">
                <description>Create a Student</description>
                <to uri="bean:studentService?method=saveEntity"/>
            </post>

            <!-- this is a rest GET to view a student by the given id -->
            <get uri="/{id}" id="route-getStudent" outType="cn.com.rexen.bean.entities.StudentBean">
                <description>Find Student by id</description>
                <to uri="bean:studentService?method=getEntity(${header.id})"/>
            </get>

            <!-- this is a rest PUT to update a student -->
            <put uri="/{id}" id="route-updateStudent" type="cn.com.rexen.bean.entities.StudentBean"
                 outType="cn.com.rexen.core.api.biz.JsonStatus">
                <description>Updates a Student</description>
                <to uri="bean:studentService?method=updateEntity"/>
            </put>

            <!-- this is a rest DELETE to delete a student -->
            <delete uri="/{id}" id="route-deleteStudent"
                outType="cn.com.rexen.core.api.biz.JsonStatus">
                <description>Delete a Student</description>
                <to uri="bean:studentService?method=deleteEntity(${header.id})"/>
            </delete>

        </rest>
    </camelContext>
</blueprint>